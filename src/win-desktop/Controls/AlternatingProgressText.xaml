<UserControl x:Class="Hope.Desktop.Controls.AlternatingProgressText"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="24" d:DesignWidth="120">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Stretch">
        <TextBlock x:Name="PrimaryText"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   TextAlignment="Center" TextTrimming="CharacterEllipsis"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
        <TextBlock x:Name="SecondaryText"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   TextAlignment="Center" TextTrimming="CharacterEllipsis" Opacity="0"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
    </Grid>
</UserControl>
