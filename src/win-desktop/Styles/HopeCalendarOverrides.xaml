<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Hope 日历高亮覆盖（增强版）
        1. 本月日期：IsInactive=False 时高亮背景 + 强调色字体
        2. 今天：IsToday=True 时实心强调色背景 + 高对比字体 + 粗体 + 下划线
        3. 选中日期：2.5px 描边高亮
        4. 跨月日期：IsInactive=True 时淡化显示
        所有颜色均取自 WPF-UI 主题资源，自动跟随深浅主题与系统强调色变化。
    -->

    <!-- 本月日期：淡强调色背景 -->
    <SolidColorBrush
        x:Key="HopeCalendarCurrentMonthBackgroundBrush"
        Color="{DynamicResource AccentFillColorDefault}"
        Opacity="0.35" />

    <!-- 本月日期：强调色字体 -->
    <SolidColorBrush
        x:Key="HopeCalendarCurrentMonthForegroundBrush"
        Color="{DynamicResource AccentFillColorDefault}" />

    <!-- 今天：实心强调色背景 -->
    <SolidColorBrush
        x:Key="HopeCalendarTodayBackgroundBrush"
        Color="{DynamicResource AccentFillColorDefault}" />

    <!-- 今天：背景上的高对比文字（WPF-UI 自动根据强调色明暗返回黑/白） -->
    <SolidColorBrush
        x:Key="HopeCalendarTodayForegroundBrush"
        Color="{DynamicResource TextOnAccentFillColorPrimary}" />

    <Style
        x:Key="HopeCalendarDayButtonStyle"
        BasedOn="{StaticResource DefaultCalendarDayButtonStyle}"
        TargetType="{x:Type CalendarDayButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarDayButton}">
                    <Grid>
                        <!-- 本月背景放在最底层， today 实心圆在其上方 -->
                        <Rectangle
                            x:Name="CurrentMonthBackground"
                            Fill="{StaticResource HopeCalendarCurrentMonthBackgroundBrush}"
                            Opacity="0"
                            RadiusX="99"
                            RadiusY="99" />
                        <Rectangle
                            x:Name="TodayBackground"
                            Fill="{DynamicResource CalendarViewTodayBackground}"
                            Opacity="0"
                            RadiusX="99"
                            RadiusY="99" />
                        <Rectangle
                            x:Name="SelectedBackground"
                            Opacity="0"
                            RadiusX="99"
                            RadiusY="99"
                            SnapsToDevicePixels="False"
                            Stroke="{DynamicResource CalendarViewSelectedBorderBrush}"
                            StrokeThickness="1"
                            UseLayoutRounding="False" />
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" />
                        <Rectangle
                            x:Name="HighlightBackground"
                            Fill="{DynamicResource CalendarViewItemBackgroundPointerOver}"
                            Opacity="0"
                            RadiusX="99"
                            RadiusY="99" />
                        <StackPanel
                            x:Name="DayContent"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <ContentPresenter
                                x:Name="NormalText"
                                Margin="5,1,5,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                TextBlock.Foreground="{DynamicResource CalendarViewForeground}" />
                            <Rectangle
                                x:Name="TodayUnderline"
                                Width="14"
                                Height="2"
                                HorizontalAlignment="Center"
                                Fill="{StaticResource HopeCalendarTodayForegroundBrush}"
                                Opacity="0"
                                RadiusX="1"
                                RadiusY="1" />
                        </StackPanel>
                        <Rectangle
                            x:Name="DayButtonFocusVisual"
                            IsHitTestVisible="False"
                            RadiusX="0"
                            RadiusY="0"
                            Stroke="Transparent"
                            StrokeThickness="1"
                            Visibility="Collapsed" />

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup Name="CommonStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0:0:0.1" />
                                </VisualStateGroup.Transitions>
                                <VisualState Name="Normal" />
                                <VisualState Name="MouseOver">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="HighlightBackground"
                                            Storyboard.TargetProperty="Opacity"
                                            To="1"
                                            Duration="0" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState Name="Pressed">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="HighlightBackground"
                                            Storyboard.TargetProperty="Opacity"
                                            To="1"
                                            Duration="0" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="HighlightBackground"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0"
                                            Duration="0" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="NormalText"
                                            Storyboard.TargetProperty="Opacity"
                                            To="1"
                                            Duration="0" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup Name="SelectionStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" />
                                </VisualStateGroup.Transitions>
                                <VisualState Name="Unselected" />
                                <VisualState Name="Selected">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="SelectedBackground"
                                            Storyboard.TargetProperty="Opacity"
                                            To="1"
                                            Duration="0" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="SelectedBackground"
                                            Storyboard.TargetProperty="StrokeThickness"
                                            To="2.5"
                                            Duration="0" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup Name="CalendarButtonFocusStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" />
                                </VisualStateGroup.Transitions>
                                <VisualState Name="CalendarButtonFocused">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames
                                            Storyboard.TargetName="DayButtonFocusVisual"
                                            Storyboard.TargetProperty="Visibility"
                                            Duration="0">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <Visibility>Visible</Visibility>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState Name="CalendarButtonUnfocused">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames
                                            Storyboard.TargetName="DayButtonFocusVisual"
                                            Storyboard.TargetProperty="Visibility"
                                            Duration="0">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <Visibility>Collapsed</Visibility>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup Name="ActiveStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" />
                                </VisualStateGroup.Transitions>
                                <VisualState Name="Active" />
                            </VisualStateGroup>

                            <VisualStateGroup Name="DayStates">
                                <VisualStateGroup.Transitions>
                                    <VisualTransition GeneratedDuration="0" />
                                </VisualStateGroup.Transitions>
                                <VisualState Name="RegularDay" />
                                <VisualState Name="Today">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="TodayBackground"
                                            Storyboard.TargetProperty="Opacity"
                                            To="1"
                                            Duration="0" />
                                        <ObjectAnimationUsingKeyFrames
                                            Storyboard.TargetName="NormalText"
                                            Storyboard.TargetProperty="(TextBlock.Foreground)">
                                            <ObjectAnimationUsingKeyFrames.KeyFrames>
                                                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{StaticResource HopeCalendarTodayForegroundBrush}" />
                                            </ObjectAnimationUsingKeyFrames.KeyFrames>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup Name="BlackoutDayStates" />
                        </VisualStateManager.VisualStateGroups>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsToday, RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource Self}}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="SelectedBackground" Property="Stroke" Value="{Binding RelativeSource={RelativeSource AncestorType=Calendar}, Path=Background}" />
                            <Setter TargetName="SelectedBackground" Property="Margin" Value="1" />
                            <Setter TargetName="SelectedBackground" Property="StrokeThickness" Value="2.5" />
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsToday, RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="TodayBackground" Property="Fill" Value="{DynamicResource CalendarViewSelectedBackgroundPointerOver}" />
                        </MultiDataTrigger>

                        <!-- 跨月日期淡化，让本月更突出 -->
                        <DataTrigger Binding="{Binding IsInactive, RelativeSource={RelativeSource Self}}" Value="True">
                            <Setter TargetName="NormalText" Property="Opacity" Value="0.45" />
                        </DataTrigger>

                        <!-- 本月日期高亮：强调色前景 + 半透明强调色背景 -->
                        <DataTrigger Binding="{Binding IsInactive, RelativeSource={RelativeSource Self}}" Value="False">
                            <Setter TargetName="NormalText" Property="TextBlock.Foreground" Value="{StaticResource HopeCalendarCurrentMonthForegroundBrush}" />
                            <Setter TargetName="NormalText" Property="TextElement.FontWeight" Value="SemiBold" />
                            <Setter TargetName="CurrentMonthBackground" Property="Opacity" Value="1" />
                        </DataTrigger>

                        <!-- 今天额外高亮：实心强调色背景 + 粗体 + 下划线 -->
                        <DataTrigger Binding="{Binding IsToday, RelativeSource={RelativeSource Self}}" Value="True">
                            <Setter TargetName="NormalText" Property="TextElement.FontWeight" Value="Bold" />
                            <Setter TargetName="TodayUnderline" Property="Opacity" Value="1" />
                            <Setter TargetName="TodayBackground" Property="Fill" Value="{StaticResource HopeCalendarTodayBackgroundBrush}" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="HopeCalendarStyle"
        BasedOn="{StaticResource DefaultCalendarDropShadowStyle}"
        TargetType="{x:Type Calendar}">
        <Setter Property="CalendarDayButtonStyle" Value="{StaticResource HopeCalendarDayButtonStyle}" />
    </Style>

    <!-- 隐式应用到所有 DatePicker，两个选择器无需额外改动 -->
    <Style
        BasedOn="{StaticResource DefaultDatePickerStyle}"
        TargetType="{x:Type DatePicker}">
        <Setter Property="CalendarStyle" Value="{StaticResource HopeCalendarStyle}" />
    </Style>

</ResourceDictionary>
